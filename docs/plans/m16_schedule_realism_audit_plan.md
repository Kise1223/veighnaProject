# M16 Schedule Realism Audit Plan

- Extend `M15` with `rolling_lookback` training windows.
- Add `explicit_model_schedule` with deterministic file-first validation.
- Emit first-class schedule audit artifacts for no-lookahead inspection.
- Extend rolling campaign compare with schedule realism metrics and bases.
