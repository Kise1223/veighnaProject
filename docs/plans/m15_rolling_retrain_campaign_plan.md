# M15 Rolling Retrain Campaign Plan

- Reuse `M14` campaign artifacts and day-level orchestration.
- Add a file-first model schedule layer for `fixed_model` and `retrain_every_n_trade_days`.
- Track per-day resolved model, model age, and model switch events.
- Extend campaign summaries and compare outputs with model refresh metrics.
