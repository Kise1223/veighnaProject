# M13 Benchmark / Attribution Analytics

## Scope

- Add file-first benchmark reference artifacts on top of `M12` portfolio analytics.
- Add deterministic benchmark-relative analytics and cross-run benchmark comparison.
- Keep the whole milestone analytics-only, paper-only, and fully decoupled from live routing.

## Deliverables

- `libs/analytics` benchmark schemas, loaders, attribution, compare, lineage, and artifact stores
- `build_benchmark_reference`, `run_benchmark_analytics`, `compare_benchmark_analytics`, and `list_benchmark_analytics` CLIs
- README, ADR, runbook, SQL DDL, and regression tests
